import { HashLoader } from 'react-spinners';

const Spinner = () => {
  return (
    <div className="mt-[13rem]">
      <HashLoader color="#0079fe" size={100} />
    </div>
  );
};

export default Spinner;
